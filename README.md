# AutoBanner

Open `index.html` in a browser to use the banner generator.

Notes:
- Keep the `assets` and `vendor` folders beside `index.html`.
- A network connection is recommended the first time the page loads because React, Tailwind, and Google Fonts are loaded from public CDNs.
- Banner ZIP export uses the local files in `vendor`.
